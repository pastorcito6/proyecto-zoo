# proyecto Zoo
Este proyecto sirve para administrar un **zoologico**

## instalacion
para montar un proyecto instalado las dependecioas del archivo requeriments.txt usando el siguiente comando:

pip install -r requirements.txt

## saludo
espero lo disfrutes y no rompas nada.